#include <stdio.h>

int main()
{
    char str[10000];
    printf("Enter string : ");
    gets(str);

    int v = 0, c = 0;
    for (int i = 0; i < strlen(str); i++)
    {
        // str[i];
    }

    return 0;
}