#include <bits/stdc++.h>
using namespace std;
/*
a => 0(log(n) + log(p))
b => 0(log(n)
c => 0(log(n) * log;
*/
int main()
{
    int n = 10;
    for (int i = 1; i < n; i = i * 2)
    {
        for (int j = 1; j < i; j++)
        {
        }
    }
    return 0;
}