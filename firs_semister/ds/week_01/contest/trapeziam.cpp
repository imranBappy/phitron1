#include <bits/stdc++.h>
using namespace std;
int main()
{
    int a, b, h;
    cin >> a >> b >> h;
    double area = ((double(a) + double(b)) * double(h)) / 2;

    cout << area << endl;

    return 0;
}